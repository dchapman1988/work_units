%h2 New Work Unit
= form_for(@work_unit) do |f|
  = render :partial => 'shared/work_unit_form', :locals => {:f => f}
  .actions
    %div{:style => "float: left; width: auto;"}
      = f.submit
= button_to 'Cancel', work_units_path, :method => :get
%br/
